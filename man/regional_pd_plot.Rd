% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_plots.R
\name{regional_pd_plot}
\alias{regional_pd_plot}
\title{Generate regional ICE + PDP plot for single tree node}
\usage{
regional_pd_plot(
  effect,
  target.feature,
  node_num,
  color_ice,
  color_pd,
  ymin,
  ymax,
  split_condition = NULL
)
}
\arguments{
\item{effect}{A named \code{list} whose elements are \code{data.table}s /
\code{data.frame}s produced by \code{regional_pd()}; each element
contains centered ICE data for one feature and a column
\code{node}.}

\item{target.feature}{Character. The response-axis label to show
on the y-axis (e.g., \code{"\\hat{f}_j"}).}

\item{node_num}{Integer. Which node (at the chosen depth) to
visualize.}

\item{color_ice}{Color value used for ICE curves}

\item{color_pd}{Color value used for the PDP line}

\item{ymin, }{ymax Numeric limits for the y-axis (kept identical
across features so multiple plots can share a scale).}

\item{split_condition}{Optional character string appended to each
facet label, e.g. \code{"x1 > 0.3"}.}
}
\value{
A \code{list} of ggplot objects—one for every feature in the
  supplied \code{effect}.
}
\description{
Generate regional ICE + PDP plot for single tree node
}
\keyword{internal}
