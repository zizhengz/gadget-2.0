% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_tree_splitting.R
\name{split_parent_node}
\alias{split_parent_node}
\title{Evaluate and select the best binary split for all features}
\usage{
split_parent_node(
  Y,
  X,
  n.splits = 1,
  min.node.size = 40,
  optimizer,
  grid,
  objective,
  n.quantiles,
  ...
)
}
\arguments{
\item{Y}{A list or data structure representing response values
(e.g. centered ICE curves) that the supplied \code{objective}
knows how to consume.}

\item{X}{A \code{data.frame} (or matrix) of predictor variables.}

\item{n.splits}{Integer. How many split points the optimizer should
return per feature (default 1 for binary splits).}

\item{min.node.size}{Integer. Minimum number of observations that
each child node must retain.}

\item{optimizer}{A function with signature
\code{optimizer(x, y, n.splits, min.node.size, grid, objective,
n.quantiles, ...)} that returns at least
\code{$split.points} and \code{$objective.value}.}

\item{grid}{Either a numeric vector or list of candidate split
points passed untouched to \code{optimizer()}.}

\item{objective}{Objective function with interface
\code{objective(y, x, requires.x, ...)} used inside the optimizer
to score splits.}

\item{n.quantiles}{Integer specifying how many quantile-based
candidate points the optimizer should pre-compute.}

\item{...}{Further arguments passed directly to
\code{optimizer()}.}
}
\value{
A **data.table** with columns
  \describe{
    \item{feature}{Feature name.}
    \item{objective.value}{Numeric value returned by \code{objective}.}
    \item{runtime}{Elapsed time (sec) spent inside \code{optimizer()}.}
    \item{split.points}{A list column; each cell holds the vector of
      split points (may be \code{NA}).}
    \item{best.split}{Logical flag indicating which feature attains
      the minimum objective.}
  }
}
\description{
Evaluate and select the best binary split for all features
}
\keyword{internal}
