% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AleStrategy.R
\name{AleStrategy}
\alias{AleStrategy}
\title{AleStrategy: Generalized Additive Decomposition Based on ALE Effects}
\format{
[R6::R6Class] object inheriting from [EffectStrategy].
}
\description{
ALE-based effect strategy (inherits from \link{EffectStrategy}). Given model and data,
preprocesses to Z/Y via \code{prepare_split_data_ale};
transforms ALE effects per node; computes ALE-derivative heterogeneity;
finds best split via \code{search_best_split_ale}; fits tree and plots ALE curves.
}
\details{
Intended for use through \code{GadgetTree$new(strategy = AleStrategy$new())} and
\code{tree$fit(...)}. Can be instantiated directly for custom pipelines.
}
\section{Construction}{

```
s = AleStrategy$new()
```
}

\examples{
\dontrun{
ale_strat = AleStrategy$new()
tree = GadgetTree$new(strategy = ale_strat, n_split = 2)
tree$fit(model = model, data = data, target_feature_name = "y")
}

}
\seealso{
\code{\link{calculate_ale_heterogeneity_cpp}}
}
\section{Super class}{
\code{\link[xplaineff:EffectStrategy]{EffectStrategy}} -> \code{AleStrategy}
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{model}}{(`any`) \cr
Fitted model (persistent after \code{$fit()}).}

    \item{\code{data}}{(`data.frame()` or `data.table()`) \cr
Data (persistent after \code{$fit()}).}

    \item{\code{target_feature_name}}{(`character(1)`) \cr
Target variable name.}

    \item{\code{n_intervals}}{(`integer(1)`) \cr
Intervals for numeric ALE.}

    \item{\code{predict_fun}}{(`function()`) \cr
\code{function(model, data)} returning predictions.}

    \item{\code{order_method}}{(`character(1)`) \cr
Categorical order: \code{"mds"}, \code{"pca"}, \code{"random"}, \code{"raw"}.}

    \item{\code{ale_engine}}{(`character(1)`) \cr
ALE backend after \code{$fit()}: \code{"cpp"} or \code{"r"}.}

    \item{\code{effect}}{(`list()` or `NULL`) \cr
Cached ALE effect used when \code{$plot()} omits \code{effect}.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-AleStrategy-initialize}{\code{AleStrategy$new()}}
    \item \href{#method-AleStrategy-preprocess}{\code{AleStrategy$preprocess()}}
    \item \href{#method-AleStrategy-node_transform}{\code{AleStrategy$node_transform()}}
    \item \href{#method-AleStrategy-heterogeneity}{\code{AleStrategy$heterogeneity()}}
    \item \href{#method-AleStrategy-get_child_objectives}{\code{AleStrategy$get_child_objectives()}}
    \item \href{#method-AleStrategy-find_best_split}{\code{AleStrategy$find_best_split()}}
    \item \href{#method-AleStrategy-plot}{\code{AleStrategy$plot()}}
    \item \href{#method-AleStrategy-fit}{\code{AleStrategy$fit()}}
    \item \href{#method-AleStrategy-clean}{\code{AleStrategy$clean()}}
    \item \href{#method-AleStrategy-clone}{\code{AleStrategy$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-initialize}{}}}
\subsection{\code{AleStrategy$new()}}{
  Create an AleStrategy instance (calls \code{super$initialize("ale")}).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$new()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-preprocess"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-preprocess}{}}}
\subsection{\code{AleStrategy$preprocess()}}{
  Preprocess to Z and Y via \code{prepare_split_data_ale}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$preprocess(
  model,
  effect = NULL,
  data,
  target_feature_name,
  n_intervals,
  feature_set = NULL,
  split_feature = NULL,
  predict_fun = NULL,
  order_method = "raw",
  ale_engine = c("cpp", "r")
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{model}}{(`any`) \cr
Fitted model.}
      \item{\code{effect}}{(`list()` or `NULL`) \cr
Reserved for future extension. Currently unsupported.}
      \item{\code{data}}{(`data.frame()` or `data.table()`) \cr
Data.}
      \item{\code{target_feature_name}}{(`character(1)`) \cr
Target variable name.}
      \item{\code{n_intervals}}{(`integer(1)`) \cr
Intervals for numeric ALE.}
      \item{\code{feature_set}}{(`character()` or `NULL`) \cr
Features for ALE; \code{NULL} = all.}
      \item{\code{split_feature}}{(`character()` or `NULL`) \cr
Features for splitting; \code{NULL} = all.}
      \item{\code{predict_fun}}{(`function()` or `NULL`) \cr
Prediction function.}
      \item{\code{order_method}}{(`character(1)`) \cr
Categorical order: \code{"mds"}, \code{"pca"}, \code{"random"}, or \code{"raw"}.}
      \item{\code{ale_engine}}{(`character(1)`) \cr
ALE engine: \code{"cpp"} or \code{"r"}; default \code{c("cpp", "r")} resolves to
\code{"cpp"} via \code{match.arg}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()`) \cr
  \code{Z}: split features; \code{Y}: ALE effect data.tables.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-node_transform"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-node_transform}{}}}
\subsection{\code{AleStrategy$node_transform()}}{
  Subset ALE by node indices; handle single-interval and categorical.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$node_transform(Y, idx, grid = NULL, is_child = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{Y}}{(`list()`) \cr
ALE effect list from \code{calculate_ale}.}
      \item{\code{idx}}{(`integer()`) \cr
Row indices in the node.}
      \item{\code{grid}}{(`list()` or `NULL`) \cr
Ignored for ALE; required by interface.}
      \item{\code{is_child}}{(`logical(1)`) \cr
Whether the current node is a child node.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()`) \cr
  Transformed ALE data.tables.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-heterogeneity"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-heterogeneity}{}}}
\subsection{\code{AleStrategy$heterogeneity()}}{
  Compute ALE heterogeneity via \code{calculate_ale_heterogeneity_cpp}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$heterogeneity(Y)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{Y}}{(`list()`) \cr
ALE effect list from \code{calculate_ale}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`numeric()`) \cr
  Heterogeneity per feature.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-get_child_objectives"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-get_child_objectives}{}}}
\subsection{\code{AleStrategy$get_child_objectives()}}{
  Compute left/right child objective values from split result.
For ALE, extracts from split_info (computed during sweep).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$get_child_objectives(
  Z,
  Y,
  split_info,
  idx_left,
  idx_right,
  grid_left,
  grid_right
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{Z}}{(`data.frame()` or `data.table()`) \cr
Split features.}
      \item{\code{Y}}{(`list()`) \cr
ALE effect from \code{calculate_ale}.}
      \item{\code{split_info}}{(`list()`) \cr
Split metadata.}
      \item{\code{idx_left, idx_right}}{(`integer()`) \cr
Child row indices.}
      \item{\code{grid_left, grid_right}}{(`list()`) \cr
Child grids.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()`) \cr
  \code{left_objective_value_j}, \code{right_objective_value_j},
  \code{left_objective_value}, \code{right_objective_value}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-find_best_split"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-find_best_split}{}}}
\subsection{\code{AleStrategy$find_best_split()}}{
  Find best split via \code{search_best_split_ale}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$find_best_split(Z, Y, min_node_size, n_quantiles)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{Z}}{(`data.frame()` or `data.table()`) \cr
Split features.}
      \item{\code{Y}}{(`list()`) \cr
ALE effect from \code{calculate_ale}.}
      \item{\code{min_node_size}}{(`integer(1)`) \cr
Minimum node size.}
      \item{\code{n_quantiles}}{(`integer(1)` or `NULL`) \cr
Quantile candidates for numeric.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()` or `data.frame()`) \cr
  Best split info: \code{split_feature}, \code{split_point}, etc.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-plot"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-plot}{}}}
\subsection{\code{AleStrategy$plot()}}{
  Plot ALE curves via \code{plot_tree_ale}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$plot(
  tree,
  effect = NULL,
  data,
  target_feature_name,
  depth = NULL,
  node_id = NULL,
  features = NULL,
  show_plot = TRUE,
  show_point = TRUE,
  mean_center = TRUE,
  ...
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{tree}}{(`list()`) \cr
Depth-based list of Node objects.}
      \item{\code{effect}}{(`list()` or `NULL`) \cr
ALE effect; \code{NULL} = use cached \code{effect}.}
      \item{\code{data}}{(`data.frame()` or `data.table()`) \cr
Data.}
      \item{\code{target_feature_name}}{(`character(1)`) \cr
Target variable name.}
      \item{\code{depth}}{(`integer()` or `NULL`) \cr
Depths to plot.}
      \item{\code{node_id}}{(`integer()` or `NULL`) \cr
Node IDs to plot.}
      \item{\code{features}}{(`character()` or `NULL`) \cr
Features to include.}
      \item{\code{show_plot, show_point, mean_center}}{(`logical(1)`) \cr
Plot options.}
      \item{\code{...}}{Passed to \code{plot_tree_ale}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()`) \cr
  Nested list (depth -> node -> patchwork).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-fit"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-fit}{}}}
\subsection{\code{AleStrategy$fit()}}{
  Fit tree: preprocess, create root, split recursively.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$fit(
  tree,
  model,
  effect = NULL,
  data,
  target_feature_name,
  n_intervals = 10,
  feature_set = NULL,
  split_feature = NULL,
  predict_fun = NULL,
  order_method = "raw",
  ale_engine = c("cpp", "r"),
  verbose = 0,
  ...
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{tree}}{(`GadgetTree`) \cr
Tree instance.}
      \item{\code{model}}{(`any`) \cr
Fitted model.}
      \item{\code{effect}}{(`list()` or `NULL`) \cr
Reserved for future extension. Currently unsupported.}
      \item{\code{data}}{(`data.frame()` or `data.table()`) \cr
Data.}
      \item{\code{target_feature_name}}{(`character(1)`) \cr
Target name.}
      \item{\code{n_intervals}}{(`integer(1)`) \cr
Intervals for numeric ALE.}
      \item{\code{feature_set, split_feature}}{(`character()` or `NULL`) \cr
Feature subsets.}
      \item{\code{predict_fun}}{(`function()` or `NULL`) \cr
Prediction function.}
      \item{\code{order_method}}{(`character(1)`) \cr
Categorical order.}
      \item{\code{ale_engine}}{(`character(1)`) \cr
ALE engine: \code{"cpp"} or \code{"r"}; default \code{c("cpp", "r")} resolves to
\code{"cpp"} via \code{match.arg}.}
      \item{\code{...}}{Ignored.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`GadgetTree`) \cr
  The tree, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-clean"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-clean}{}}}
\subsection{\code{AleStrategy$clean()}}{
  Sets \code{data} and \code{model} to \code{NULL} to free memory after fitting.
\code{effect} is intentionally retained because \code{plot()} requires it post-fit.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$clean()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AleStrategy-clone"></a>}}
\if{latex}{\out{\hypertarget{method-AleStrategy-clone}{}}}
\subsection{\code{AleStrategy$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AleStrategy$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
