% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GadgetTree.R
\name{GadgetTree}
\alias{GadgetTree}
\title{GadgetTree: Generalized Additive Decomposition of Global Effects Tree}
\format{
[R6::R6Class] object.
}
\description{
Wrapper for effect-based trees: given a strategy (pd/ale), fits tree via
\code{$fit()}, plots effects via \code{$plot()}, extracts splits via
\code{$extract_split_info()}.
Delegates all effect logic to the strategy.
}
\details{
This class manages the overall tree structure and delegates effect-specific operations
(such as splitting and plotting) to the provided strategy object. It is the main entry
point for fitting and visualizing effect-based decision trees in the xplaineff package.
}
\section{Construction}{

```
t = GadgetTree$new(strategy, n_split = 2, impr_par = 0.1, min_node_size = 10, n_quantiles = NULL)
```
* `strategy` :: ([AleStrategy] | [PdStrategy])\cr
  Effect strategy object.
* `n_split` :: `integer(1)`\cr
  Maximum split depth, counted as splits along any root-to-leaf path.
* `impr_par` :: `numeric(1)`\cr
  Improvement threshold.
* `min_node_size` :: `integer(1)`\cr
  Minimum samples per node.
* `n_quantiles` :: `integer(1)` or `NULL`\cr
  Number of quantile cut points for numeric split candidates; `NULL` = use all unique values.
}

\examples{
\dontrun{
# PD: `data` and `target_feature_name` come first; pass `effect` and/or `model` via `...`.
pd_strat = PdStrategy$new()
tree = GadgetTree$new(strategy = pd_strat, n_split = 2)
tree$fit(data = dat, target_feature_name = "y", effect = effect_obj)
tree$plot(data = dat, target_feature_name = "y", effect = effect_obj)
tree$plot(data = dat, target_feature_name = "y")  # uses cached effect from fit()
tree$plot_tree_structure()
split_info = tree$extract_split_info()

# ALE: pass `model` (and optional `n_intervals`, `ale_engine`, etc.).
tree_ale = GadgetTree$new(strategy = AleStrategy$new(), n_split = 2)
tree_ale$fit(data = dat, target_feature_name = "y", model = fitted_model)
tree_ale$plot(data = dat, target_feature_name = "y")
}

}
\keyword{internal}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{strategy}}{(PdStrategy | AleStrategy) \cr
Effect-specific logic.}

    \item{\code{root}}{(`Node`) \cr
Root node.}

    \item{\code{n_split}}{(`integer(1)`) \cr
Maximum split depth, counted as splits along any root-to-leaf path.}

    \item{\code{impr_par}}{(`numeric(1)`) \cr
Improvement threshold.}

    \item{\code{min_node_size}}{(`integer(1)`) \cr
Minimum samples per node.}

    \item{\code{n_quantiles}}{(`integer(1)` or `NULL`) \cr
Number of quantile cut points for numeric split candidates.}

    \item{\code{split_benchmark}}{(`list()`) \cr
Internal split timing records.}

    \item{\code{tree_list_cache}}{(`list()` or `NULL`) \cr
Cached depth-based tree list; invalidated on \code{$fit()}.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-GadgetTree-initialize}{\code{GadgetTree$new()}}
    \item \href{#method-GadgetTree-fit}{\code{GadgetTree$fit()}}
    \item \href{#method-GadgetTree-plot}{\code{GadgetTree$plot()}}
    \item \href{#method-GadgetTree-plot_tree_structure}{\code{GadgetTree$plot_tree_structure()}}
    \item \href{#method-GadgetTree-extract_split_info}{\code{GadgetTree$extract_split_info()}}
    \item \href{#method-GadgetTree-get_tree_list}{\code{GadgetTree$get_tree_list()}}
    \item \href{#method-GadgetTree-clone}{\code{GadgetTree$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-initialize}{}}}
\subsection{\code{GadgetTree$new()}}{
  Initialize tree parameters.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$new(
  strategy,
  n_split = 2,
  impr_par = 0.1,
  min_node_size = 10,
  n_quantiles = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{strategy}}{(PdStrategy | AleStrategy) \cr
Strategy object.}
      \item{\code{n_split}}{(`integer(1)`) \cr
Maximum split depth, counted as splits along any root-to-leaf path.}
      \item{\code{impr_par}}{(`numeric(1)`) \cr
Improvement threshold.}
      \item{\code{min_node_size}}{(`integer(1)`) \cr
Minimum node size.}
      \item{\code{n_quantiles}}{(`integer(1)` or `NULL`) \cr
Number of quantile cut points for numeric split candidates.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-fit"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-fit}{}}}
\subsection{\code{GadgetTree$fit()}}{
  Fit tree via \code{strategy$fit()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$fit(
  data,
  target_feature_name,
  feature_set = NULL,
  split_feature = NULL,
  gadget_improvements = NULL,
  gadget_impr_args = NULL,
  verbose = 0,
  ...
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{data}}{(`data.frame()`) \cr
Data with features and target.}
      \item{\code{target_feature_name}}{(`character(1)`) \cr
Target name.}
      \item{\code{feature_set}}{(`character()` or `NULL`) \cr
Features for effect; \code{NULL} = all.}
      \item{\code{split_feature}}{(`character()` or `NULL`) \cr
Features for splitting; \code{NULL} = all.}
      \item{\code{gadget_improvements}}{(`character(1)` or `NULL`) \cr
Optional selective early stopping method, which dynamically drops non-interacting
features from the split-candidate set Z and the risk set S. One of
\code{"plain_risk"} (Method 1: absolute normalized risk),
\code{"risk_reduction"} (Method 2: relative risk reduction of the split; being
reduction-based it cannot drop features before the first split), or
\code{"interaction_fraction"} (Method 3: interaction share
\eqn{R_j / (R_j + B_j + \delta)} of the feature's own local-effect variance).
\code{NULL} disables it. PD strategy only.}
      \item{\code{gadget_impr_args}}{(`list()`) \cr
Named list of method-specific arguments for \code{gadget_improvements}:
\code{tau} (`numeric(1)`, default 0.05) for all methods, and \code{delta}
(`numeric(1)`, default 1e-12) regularizing the denominator of Method 3.}
      \item{\code{...}}{(`list()`) \cr
Strategy-specific arguments passed to \code{$fit()}.
For [AleStrategy]: \code{model} or \code{effect}, plus optional
\code{n_intervals}, \code{predict_fun}, \code{order_method},
and \code{ale_engine}.
For [PdStrategy]: \code{effect}, or \code{model} with optional
\code{predict_fun}, \code{n_grid}, and \code{pd_engine}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`GadgetTree`) \cr
  The tree, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-plot"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-plot}{}}}
\subsection{\code{GadgetTree$plot()}}{
  Plot tree via \code{strategy$plot()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$plot(
  data,
  target_feature_name,
  effect = NULL,
  depth = NULL,
  node_id = NULL,
  features = NULL,
  ...
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{data}}{(`data.frame()`) \cr
Data.}
      \item{\code{target_feature_name}}{(`character(1)`) \cr
Target name.}
      \item{\code{effect}}{(R6 or `list()` or `NULL`) \cr
Optional effect object; omit or \code{NULL} uses strategy-cached effect from \code{$fit()}.}
      \item{\code{depth}}{(`integer()` or `NULL`) \cr
Depths to plot.}
      \item{\code{node_id}}{(`integer()` or `NULL`) \cr
Node IDs to plot.}
      \item{\code{features}}{(`character()` or `NULL`) \cr
Features to plot.}
      \item{\code{...}}{Plot arguments.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()`) \cr
  Nested list (depth -> node -> patchwork).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-plot_tree_structure"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-plot_tree_structure}{}}}
\subsection{\code{GadgetTree$plot_tree_structure()}}{
  Converts root to depth-list and calls \code{plot_tree_structure()}. Prints graph.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$plot_tree_structure(
  label_wrap_width = 34L,
  node_spread_x = 1.55,
  node_spread_y = 1.12
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{label_wrap_width}}{(`integer(1)` or `NULL`) \cr
Wrap node labels to this many characters per line; \code{NULL} disables wrapping.}
      \item{\code{node_spread_x, node_spread_y}}{(`numeric(1)`) \cr
Layout stretch factors for the ggraph \code{"tree"} layout (larger values separate nodes).}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-extract_split_info"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-extract_split_info}{}}}
\subsection{\code{GadgetTree$extract_split_info()}}{
  Extract split info from tree.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$extract_split_info(include_timing = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{include_timing}}{(`logical(1)`) \cr
Whether to include internal split timings in the output.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`data.frame()`) \cr
  Split info: depth, id, split_feature, split_value, int_imp, etc.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-get_tree_list"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-get_tree_list}{}}}
\subsection{\code{GadgetTree$get_tree_list()}}{
  Get depth-based tree list (cached). Invalidated on \code{$fit()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$get_tree_list()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    (`list()`) \cr
  Depth-based list of nodes.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-GadgetTree-clone"></a>}}
\if{latex}{\out{\hypertarget{method-GadgetTree-clone}{}}}
\subsection{\code{GadgetTree$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{GadgetTree$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
